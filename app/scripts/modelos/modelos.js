/*'use scrict';
(function(){
var User = function(username, password, rol){
  this.username = username;
  this.password = password;
  this.rol = rol;
};

var Activity = function(id, desc, monto, status) {
  this.id = id;
  this.description = desc;
  this.monto = monto;
  this.status = status;
};

var Location = function(lat, lng) {
  this.latitude = lat;
  this.longitude = lng;
};

var County = function(nombre, loc, activities) {
  this.nombre = nombre;
  this.location = loc;
  this.activities = activities;
};

} */
